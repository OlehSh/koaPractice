export enum PROFILE_TYPE {
  ADMIN = 1,
  ARCHITECTOR = 2
}
export enum CONTENT_TYPE {
  JSON = 'json',
  TEXT = 'text',
  MULTIPART = 'multipart'
}

export enum RELATION_DIRECTION {
  TWO_WAY = 0,
  OUT = 1,
  IN = 2
}